// Re-exported from the shared cover-card-style module so every page
// gets the same hook and class-name helpers from one source of truth.
export { useCoverCardStyle, coverCardContainerClass, coverCardSurfaceClass } from "../lib/coverCardStyle";
